#include "shell.h"

/**
 * main - entry point simple shell project
 * @args: the arg count
 * @arg the arg vector
 *
 * Return: 0 on success, 1 on error
 */

int main(int args, char **arg)
{
	return (0);
}
